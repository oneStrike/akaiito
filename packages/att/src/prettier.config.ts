import type { Config } from 'prettier'

/* prettier config */
export default <Config>{
  useTabs: false,
  tabWidth: 2,
  printWidth: 120,
  singleQuote: true,
  semi: false,
  trailingComma: 'all',
  arrowParens: 'always',
  proseWrap: 'always',
  quoteProps: 'preserve',
}
