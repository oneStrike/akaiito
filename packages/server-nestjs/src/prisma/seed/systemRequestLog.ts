export async function createInitialSystemRequestLog(prisma: any) {
  // 系统请求日志通常是运行时生成的数据，不需要初始化种子数据
  // 这里只是为了保持种子文件的完整性，实际不插入任何数据
  console.log(
    'SystemRequestLog seed: 请求日志数据由系统运行时自动生成，无需初始化种子数据',
  )
}
